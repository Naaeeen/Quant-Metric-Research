name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    env:
      OMP_NUM_THREADS: "1"
      OPENBLAS_NUM_THREADS: "1"
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: |
            pyproject.toml
            examples/colab-constraints.txt
      - name: Update the isolated installer
        run: python -m pip install --upgrade "pip==26.2.1"
      - name: Install package and verification tools
        if: matrix.python-version != '3.12'
        run: python -m pip install ".[dev]" "build>=1.2,<2"
      - name: Install constrained Colab research environment
        if: matrix.python-version == '3.12'
        run: python -m pip install ".[dev,notebook]" "build>=1.2,<2" -c examples/colab-constraints.txt
      - name: Check installed dependencies
        run: python -m pip check
      - name: Record resolved dependency versions
        run: python -m pip freeze
      - name: Lint
        run: python -m ruff check .
      - name: Test with configured coverage gate
        run: python -m pytest
      - name: Check installed command
        run: qmr --help
      - name: Build source and wheel distributions
        run: python -m build
